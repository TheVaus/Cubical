//! `cubical-ast` — the canonical Markdown AST.
//!
//! Skeleton in L0. The full enum lands in L1 alongside Lezer→canonical
//! normalization. Intentionally slim: only nodes Cubical itself produces.
//! See `docs/architecture.md` §5.5.

#![forbid(unsafe_code)]
#![warn(missing_docs)]
