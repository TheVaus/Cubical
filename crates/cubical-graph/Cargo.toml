[package]
name = "cubical-graph"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Vault link graph: builds a node/edge model from the index and lays it out with a deterministic force simulation. No Tauri, no IPC, no UI."

[dependencies]
cubical-ast = { path = "../cubical-ast" }
cubical-index = { path = "../cubical-index" }
libsql.workspace = true
serde = { workspace = true, features = ["derive"] }
thiserror.workspace = true

[dev-dependencies]
tokio.workspace = true
tempfile.workspace = true
