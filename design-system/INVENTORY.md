<!-- generated by scripts/gen_ds_inventory.py — do not edit -->

# Design system component inventory

Every component under `design-system/src/components` — 26 of them. **Check this list before hand-rolling a control** ([`../docs/principles/design-system.md`](../docs/principles/design-system.md)).

Regenerate with `python3 scripts/gen_ds_inventory.py`; `--check` fails if the file is stale. Names, import paths and props are parsed from the source. Purposes are **hand-curated** in the generator — this repo bans comments, so there is no doc-comment to read — and a component with no entry says so instead of guessing.

| Category | Components |
|---|---|
| `brand/` | `CubeMark` |
| `data/` | `BacklinkRow`, `FileIcon`, `FileTreeRow`, `FolderTreeRow`, `RenameInput`, `Tag` |
| `feedback/` | `Badge`, `Callout`, `Toast`, `Tooltip` |
| `forms/` | `Button`, `DatePicker`, `IconButton`, `Link`, `SegmentedControl`, `Select`, `TextInput`, `Toggle` |
| `graphics/` | `Icon` |
| `overlay/` | `CommandPalette`, `ConfirmDialog`, `Menu`, `Modal`, `Popover`, `TwoPaneModal` |

## `brand/`

### CubeMark

The Cubical cube logo, drawn inline as SVG at a caller-chosen size.

```ts
import CubeMark from "@ds/components/brand/CubeMark/CubeMark";
```

| Prop | Type | Required |
|---|---|---|
| `size` | `number` | no |

## `data/`

### BacklinkRow

One backlink result — note title over a snippet with the matched query wrapped in `<mark>`.

```ts
import BacklinkRow from "@ds/components/data/BacklinkRow/BacklinkRow";
```

| Prop | Type | Required |
|---|---|---|
| `noteTitle` | `string` | yes |
| `snippet` | `string` | yes |
| `matchQuery` | `string` | no |
| `onClick` | `() => void` | no |

### FileIcon

File-kind glyph (folder, md, txt, png, svg, pdf, code, canvas, broken) for a tree row.

```ts
import FileIcon from "@ds/components/data/FileTreeRow/FileIcon";
```

| Prop | Type | Required |
|---|---|---|
| `kind` | `FileKind` | yes |

Also exports:

```ts
export type FileKind = 'folder' | 'folder-open' | 'md' | 'txt' | 'png' | 'svg' | 'pdf' | 'code' | 'canvas' | 'broken';
```

### FileTreeRow

One vault file-tree row: indent depth, kind icon, selected/invalid state, inline rename.

```ts
import FileTreeRow from "@ds/components/data/FileTreeRow/FileTreeRow";
```

| Prop | Type | Required |
|---|---|---|
| `name` | `string` | yes |
| `depth` | `number` | yes |
| `ext` | `string \| undefined` | no |
| `kind` | `FileKind` | no |
| `height` | `number` | no |
| `selected` | `boolean` | no |
| `invalid` | `boolean` | no |
| `unsupported` | `boolean` | no |
| `nameTitle` | `string \| undefined` | no |
| `badge` | `JSX.Element` | no |
| `renaming` | `boolean` | no |
| `focusable` | `boolean` | no |
| `role` | `'option' \| 'treeitem'` | no |
| `onClick` | `() => void` | no |
| `onContextMenu` | `(e: MouseEvent) => void` | no |
| `onRenameCommit` | `(name: string) => void` | no |
| `onRenameCancel` | `() => void` | no |

### FolderTreeRow

_not inferred — add an entry to `PURPOSES` in `scripts/gen_ds_inventory.py`_

```ts
import FolderTreeRow from "@ds/components/data/FileTreeRow/FolderTreeRow";
```

| Prop | Type | Required |
|---|---|---|
| `name` | `string` | yes |
| `depth` | `number` | yes |
| `collapsed` | `boolean` | yes |
| `height` | `number` | no |
| `renaming` | `boolean` | no |
| `onToggle` | `() => void` | no |
| `onContextMenu` | `(e: MouseEvent) => void` | no |
| `onRenameCommit` | `(name: string) => void` | no |
| `onRenameCancel` | `() => void` | no |

### RenameInput

_not inferred — add an entry to `PURPOSES` in `scripts/gen_ds_inventory.py`_

```ts
import RenameInput from "@ds/components/data/FileTreeRow/RenameInput";
```

| Prop | Type | Required |
|---|---|---|
| `value` | `string` | yes |
| `onCommit` | `(name: string) => void` | yes |
| `onCancel` | `() => void` | yes |

### Tag

Pill container for a label — optionally clickable, editable and removable.

```ts
import Tag from "@ds/components/data/Tag/Tag";
```

| Prop | Type | Required |
|---|---|---|
| `label` | `string` | yes |
| `tag` | `boolean` | no |
| `onClick` | `() => void` | no |
| `clickTitle` | `string` | no |
| `onEdit` | `(() => void) \| undefined` | no |
| `onRemove` | `() => void` | no |
| `removeTitle` | `string` | no |
| `class` | `string` | no |
| `style` | `JSX.CSSProperties \| string` | no |

## `feedback/`

### Badge

Small inline status label in one of the four tones.

```ts
import Badge from "@ds/components/feedback/Badge/Badge";
```

| Prop | Type | Required |
|---|---|---|
| `tone` | `Tone` | no |
| `children` | `JSX.Element` | yes |

Also exports:

```ts
export type Tone = 'neutral' | 'success' | 'warning' | 'error';
```

### Callout

Block notice with a tone, an optional title and arbitrary body content.

```ts
import Callout from "@ds/components/feedback/Callout/Callout";
```

| Prop | Type | Required |
|---|---|---|
| `tone` | `Tone` | no |
| `title` | `string` | no |
| `children` | `JSX.Element` | yes |

Also exports:

```ts
export type Tone = 'neutral' | 'success' | 'warning' | 'error';
```

### Toast

Transient notification with a tone, a dismiss control and auto-dismiss after `autoDismissMs`.

```ts
import Toast from "@ds/components/feedback/Toast/Toast";
```

| Prop | Type | Required |
|---|---|---|
| `tone` | `Tone` | no |
| `message` | `string` | yes |
| `onDismiss` | `() => void` | yes |
| `autoDismissMs` | `number` | no |

Also exports:

```ts
export type Tone = 'neutral' | 'success' | 'warning' | 'error';
```

### Tooltip

Hover/focus label positioned on one of four sides of its child.

```ts
import Tooltip from "@ds/components/feedback/Tooltip/Tooltip";
```

| Prop | Type | Required |
|---|---|---|
| `label` | `string` | yes |
| `placement` | `'top' \| 'bottom' \| 'left' \| 'right'` | no |
| `children` | `JSX.Element` | yes |

## `forms/`

### Button

Text button in four variants (primary/secondary/ghost/danger) and two sizes.

```ts
import Button from "@ds/components/forms/Button/Button";
```

| Prop | Type | Required |
|---|---|---|
| `variant` | `'primary' \| 'secondary' \| 'ghost' \| 'danger'` | no |
| `size` | `'sm' \| 'md'` | no |
| `disabled` | `boolean` | no |
| `type` | `'button' \| 'submit'` | no |
| `fullWidth` | `boolean` | no |
| `block` | `boolean` | no |
| `ariaLabel` | `string` | no |
| `title` | `string` | no |
| `ariaExpanded` | `boolean` | no |
| `ariaPressed` | `boolean` | no |
| `style` | `JSX.CSSProperties` | no |
| `onClick` | `(e: MouseEvent) => void` | no |
| `children` | `JSX.Element` | yes |

### DatePicker

Native `date` / `datetime-local` input styled to the tokens.

```ts
import DatePicker from "@ds/components/forms/DatePicker/DatePicker";
```

| Prop | Type | Required |
|---|---|---|
| `type` | `'date' \| 'datetime-local'` | no |
| `value` | `string` | yes |
| `onInput` | `((value: string) => void) \| undefined` | no |
| `onChange` | `((value: string) => void) \| undefined` | no |
| `onFocus` | `(() => void) \| undefined` | no |
| `onBlur` | `(() => void) \| undefined` | no |
| `onKeyDown` | `((e: KeyboardEvent) => void) \| undefined` | no |
| `size` | `'sm' \| 'md'` | no |
| `ariaLabel` | `string` | no |
| `ref` | `((el: HTMLInputElement) => void) \| undefined` | no |
| `class` | `string` | no |
| `style` | `JSX.CSSProperties \| string` | no |

### IconButton

Square icon-only button — `label` supplies the accessible name.

```ts
import IconButton from "@ds/components/forms/IconButton/IconButton";
```

| Prop | Type | Required |
|---|---|---|
| `label` | `string` | yes |
| `active` | `boolean` | no |
| `disabled` | `boolean` | no |
| `title` | `string` | no |
| `ariaExpanded` | `boolean` | no |
| `ariaPressed` | `boolean` | no |
| `mono` | `boolean` | no |
| `size` | `'sm' \| 'md'` | no |
| `ariaHaspopup` | `JSX.AriaAttributes['aria-haspopup']` | no |
| `style` | `JSX.CSSProperties` | no |
| `onClick` | `(e: MouseEvent) => void` | no |
| `children` | `JSX.Element` | yes |

### Link

Inline anchor-styled control; renders an `<a>` with `href`, otherwise a `<button>`.

```ts
import Link from "@ds/components/forms/Link/Link";
```

| Prop | Type | Required |
|---|---|---|
| `children` | `JSX.Element` | yes |
| `href` | `string` | no |
| `onClick` | `() => void` | no |
| `size` | `'xs' \| 'sm' \| 'md'` | no |
| `ariaLabel` | `string` | no |
| `class` | `string` | no |
| `style` | `JSX.CSSProperties \| string` | no |

### SegmentedControl

Row of mutually exclusive options rendered as tabs or a pill group.

```ts
import SegmentedControl from "@ds/components/forms/SegmentedControl/SegmentedControl";
```

| Prop | Type | Required |
|---|---|---|
| `options` | `SegmentedOption[]` | yes |
| `value` | `string` | yes |
| `variant` | `'tabs' \| 'pill'` | no |
| `role` | `'tablist' \| 'radiogroup'` | no |
| `onChange` | `(value: string) => void` | yes |

Also exports:

```ts
export interface SegmentedOption {
  label: string;
  icon?: IconName;
  value: string;
}
```

### Select

Single-choice dropdown over a list of options.

```ts
import Select from "@ds/components/forms/Select/Select";
```

| Prop | Type | Required |
|---|---|---|
| `options` | `SelectOption[]` | yes |
| `value` | `string` | yes |
| `onChange` | `(value: string) => void` | yes |
| `size` | `'sm' \| 'md'` | no |
| `ariaLabel` | `string` | no |
| `disabled` | `boolean` | no |
| `fullWidth` | `boolean` | no |
| `class` | `string` | no |
| `style` | `JSX.CSSProperties \| string` | no |

Also exports:

```ts
export interface SelectOption {
  value: string;
  label?: string;
}
```

### TextInput

Single-line text field.

```ts
import TextInput from "@ds/components/forms/TextInput/TextInput";
```

| Prop | Type | Required |
|---|---|---|
| `value` | `string` | yes |
| `onInput` | `(value: string) => void` | yes |
| `placeholder` | `string` | no |
| `disabled` | `boolean` | no |
| `type` | `string` | no |
| `ref` | `(el: HTMLInputElement) => void` | no |
| `ariaLabel` | `string` | no |
| `style` | `JSX.CSSProperties` | no |
| `size` | `'sm' \| 'md'` | no |
| `onFocus` | `() => void` | no |
| `onBlur` | `() => void` | no |
| `onKeyDown` | `(e: KeyboardEvent) => void` | no |
| `inputMode` | `string` | no |

### Toggle

Boolean switch (`role="switch"`) with a required label that can be visually hidden.

```ts
import Toggle from "@ds/components/forms/Toggle/Toggle";
```

| Prop | Type | Required |
|---|---|---|
| `checked` | `boolean` | yes |
| `onChange` | `(checked: boolean) => void` | yes |
| `disabled` | `boolean` | no |
| `label` | `string` | yes |
| `showLabel` | `boolean` | no |

## `graphics/`

### Icon

Renders one named icon from the vendored Lucide registry; decorative unless given a title or aria-label.

```ts
import Icon from "@ds/components/graphics/Icon/Icon";
```

| Prop | Type | Required |
|---|---|---|
| `name` | `IconName` | yes |
| `size` | `number` | no |
| `title` | `string` | no |
| `ariaLabel` | `string` | no |
| `class` | `string` | no |
| `style` | `JSX.CSSProperties \| string` | no |

## `overlay/`

### CommandPalette

Modal command list filtered by a substring query over a flat `Command[]`.

```ts
import CommandPalette from "@ds/components/overlay/CommandPalette/CommandPalette";
```

| Prop | Type | Required |
|---|---|---|
| `open` | `boolean` | yes |
| `onClose` | `() => void` | yes |
| `commands` | `Command[]` | yes |

Also exports:

```ts
export interface Command {
  id: string;
  label: string;
  onRun: () => void;
}
```

### ConfirmDialog

Yes/no confirmation over a `Modal` — message, cancel, and a danger or primary confirm. Swallows Escape, so a dialog opened from another overlay does not close both.

```ts
import ConfirmDialog from "@ds/components/overlay/ConfirmDialog/ConfirmDialog";
```

| Prop | Type | Required |
|---|---|---|
| `open` | `boolean` | yes |
| `title` | `string` | yes |
| `ariaLabel` | `string` | no |
| `confirmLabel` | `string` | yes |
| `cancelLabel` | `string` | no |
| `tone` | `'danger' \| 'primary'` | no |
| `busy` | `boolean` | no |
| `onConfirm` | `() => void` | yes |
| `onCancel` | `() => void` | yes |
| `children` | `JSX.Element` | yes |

### Menu

Bare dropdown list of menu items with optional shortcut hints and danger/disabled states; has no own open state, wrap it in `Popover`.

```ts
import Menu from "@ds/components/overlay/Menu/Menu";
```

| Prop | Type | Required |
|---|---|---|
| `items` | `MenuItem[]` | yes |

Also exports:

```ts
export interface MenuItem {
  id: string;
  label: string;
  shortcut?: string;
  disabled?: boolean;
  danger?: boolean;
  onSelect: () => void;
}
```

### Modal

Dialog surface in a portal — scrim, Escape-to-close, top or centered placement.

```ts
import Modal from "@ds/components/overlay/Modal/Modal";
```

| Prop | Type | Required |
|---|---|---|
| `open` | `boolean` | yes |
| `onClose` | `() => void` | yes |
| `title` | `string` | no |
| `ariaLabel` | `string` | no |
| `size` | `'sm' \| 'md'` | no |
| `placement` | `'top' \| 'center'` | no |
| `children` | `JSX.Element` | yes |

### Popover

Anchored floating surface with placement, backdrop click and Escape-to-close.

```ts
import Popover from "@ds/components/overlay/Popover/Popover";
```

| Prop | Type | Required |
|---|---|---|
| `open` | `boolean` | yes |
| `onClose` | `() => void` | yes |
| `ariaLabel` | `string` | yes |
| `placement` | `PopoverPlacement` | no |
| `class` | `string` | no |
| `children` | `JSX.Element` | yes |

Also exports:

```ts
export type PopoverPlacement = 'bottom-start' | 'bottom-end' | 'top-start' | 'top-end';
```

### TwoPaneModal

Modal with a left nav list and a right content pane.

```ts
import TwoPaneModal from "@ds/components/overlay/TwoPaneModal/TwoPaneModal";
```

| Prop | Type | Required |
|---|---|---|
| `open` | `boolean` | yes |
| `onClose` | `() => void` | yes |
| `title` | `string` | yes |
| `items` | `TwoPaneNavItem[]` | yes |
| `activeId` | `string` | yes |
| `onSelect` | `(id: string) => void` | yes |
| `ariaLabel` | `string` | no |
| `children` | `JSX.Element` | yes |

Also exports:

```ts
export interface TwoPaneNavItem {
  id: string;
  icon?: IconName;
  label: string;
}
```

## Non-component modules

Files under `components/` that export no component.

| Module | Import |
|---|---|
| `data/FileTreeRow/rowGeometry.ts` | `@ds/components/data/FileTreeRow/rowGeometry` |
| `graphics/Icon/icons.ts` | `@ds/components/graphics/Icon/icons` |
| `graphics/svg.ts` | `@ds/components/graphics/svg` |
