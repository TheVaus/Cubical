<!-- generated by scripts/gen_repo_layout.py — do not edit -->

# Repository layout (generated)

Generated by `scripts/gen_repo_layout.py` from the Cargo workspace manifests and the git index. Do not hand-edit — change the generator, or the `description` in the crate's `Cargo.toml`.

## Workspace crates

Workspace members: `crates/*` → 11 crates.

| Crate | Path | Role | Role source | Tauri-free |
|---|---|---|---|---|
| `cubical-app` | `crates/cubical-app/` | The Tauri shell — a thin frontend over cubical-engine (the actual logic). | `Cargo.toml` `description` | no |
| `cubical-ast` | `crates/cubical-ast/` | The canonical Markdown AST. Lingua franca for every system outside the editor: indexer, link resolver, exporter, plugin host. | `Cargo.toml` `description` | **yes** |
| `cubical-cli` | `crates/cubical-cli/` | Terminal frontend over cubical-engine — proof that the engine drives with no Tauri. | `Cargo.toml` `description` | **yes** |
| `cubical-core` | `crates/cubical-core/` | Vault, file watcher, file-type registry, frontmatter I/O. Core building blocks shared by the rest of Cubical, with no Tauri dependency. | `Cargo.toml` `description` | **yes** |
| `cubical-engine` | `crates/cubical-engine/` | Frontend-agnostic engine: command handlers, state, events, and IPC types. No Tauri — consumed by the Tauri shell and (future) CLI. | `Cargo.toml` `description` | **yes** |
| `cubical-graph` | `crates/cubical-graph/` | Vault link graph: builds a node/edge model from the index and lays it out with a deterministic force simulation. No Tauri, no IPC, no UI. | `Cargo.toml` `description` | **yes** |
| `cubical-index` | `crates/cubical-index/` | libSQL schema, migrations, and queries. Owns the on-disk index database at .cubical/index.db. | `Cargo.toml` `description` | **yes** |
| `cubical-ipc` | `crates/cubical-ipc/` | Wire protocol + shared dispatch between the Cubical app backend and the cubical CLI frontend. | `Cargo.toml` `description` | **yes** |
| `cubical-query` | `crates/cubical-query/` | dataview-style query parser + evaluator | carried over from `docs/README.md` | **yes** |
| `cubical-search` | `crates/cubical-search/` | Tantivy wrapper. Full-text search over the canonical AST (L4-A). | `Cargo.toml` `description` | **yes** |
| `cubical-sync` | `crates/cubical-sync/` | CRDT abstraction. Trait CrdtBackend lives here; the Loro implementation lands at L7. | `Cargo.toml` `description` | **yes** |

### Tauri coupling

The repo rule is that every crate except `cubical-app` must build and test without the Tauri harness. This table is **derived**, not asserted: a crate is Tauri-coupled if any of its `[dependencies]`, `[dev-dependencies]`, `[build-dependencies]` or `[target.'…'.dependencies]` names `tauri` or a `tauri-*` crate, or if it depends on a workspace crate that is.

| Crate | Tauri-free | Why |
|---|---|---|
| `cubical-app` | no | `tauri-build` (build-dependencies); `tauri-plugin-dialog` (dependencies); `tauri` (dependencies) |
| `cubical-ast` | **yes** | no `tauri*` dependency, direct or transitive |
| `cubical-cli` | **yes** | no `tauri*` dependency, direct or transitive |
| `cubical-core` | **yes** | no `tauri*` dependency, direct or transitive |
| `cubical-engine` | **yes** | no `tauri*` dependency, direct or transitive |
| `cubical-graph` | **yes** | no `tauri*` dependency, direct or transitive |
| `cubical-index` | **yes** | no `tauri*` dependency, direct or transitive |
| `cubical-ipc` | **yes** | no `tauri*` dependency, direct or transitive |
| `cubical-query` | **yes** | no `tauri*` dependency, direct or transitive |
| `cubical-search` | **yes** | no `tauri*` dependency, direct or transitive |
| `cubical-sync` | **yes** | no `tauri*` dependency, direct or transitive |

Tauri-free (10): `cubical-ast`, `cubical-cli`, `cubical-core`, `cubical-engine`, `cubical-graph`, `cubical-index`, `cubical-ipc`, `cubical-query`, `cubical-search`, `cubical-sync`.

Tauri-coupled (1): `cubical-app`.

## Top-level directories

Derived from the git index plus a listed set of gitignored directories — deliberately **not** from a filesystem scan, so this section reads the same on a fresh clone (where every `generated / untracked` row is absent on disk) as on a fully built tree. Purposes are curated in the generator; a directory with no curated purpose is emitted blank rather than guessed at.

| Directory | Status | Purpose |
|---|---|---|
| `.agents/` | tracked | Portable agent skills (`skills/`), runner-agnostic; `.claude/skills/` symlinks into it. |
| `.claude/` | tracked | Claude Code project config: `settings.json` (permission allowlist), `launch.json` (dev-server configs), `skills/` (symlinks into `.agents/skills/`). |
| `.github/` | tracked | CI workflow (runs `scripts/check.sh`) + Dependabot config. |
| `.superpowers/` | generated / untracked | Session scaffolding for the superpowers workflow: brainstorm server state and `sdd/` progress/review artifacts. |
| `.vite/` | generated / untracked | Vite dependency pre-bundling cache. |
| `crates/` | tracked | The Rust workspace — one directory per crate (see the crate table above). |
| `design-system/` | tracked | `@ds` — SolidJS component library + canonical design tokens; `ui/` borrows from here. |
| `docs/` | tracked | The documentation tree; start at `docs/README.md`. |
| `node_modules/` | generated / untracked | npm dependency tree (root-level; `ui/` and `design-system/` have their own). |
| `scripts/` | tracked | `check.sh` (the gate set), `check_docs.py`, and these generators. |
| `target/` | generated / untracked | Cargo build output for the whole workspace. |
| `ui/` | tracked | Solid + TypeScript + Vite frontend. |

## Notable nested directories

| Directory | Status | Purpose |
|---|---|---|
| `.agents/skills/` | tracked | Skill definitions, vendored and pinned by `skills-lock.json`; each carries its own `Local edits` section describing how it diverges from upstream. |
| `.claude/agents/` | tracked | Subagent definitions (`explorer`, `implementer`, `verifier`) the session contract dispatches to. |
| `.claude/skills/` | tracked | Symlinks into `.agents/skills/` so Claude Code picks them up. |
| `.claude/worktrees/` | generated / untracked | Scratch git worktrees. Contains full repo checkouts — exclude it from any recursive search. |
| `.github/ISSUE_TEMPLATE/` | tracked | Issue forms — the taxonomy is owned by `docs/principles/sessions.md`. |
| `.github/workflows/` | tracked | GitHub Actions definitions. |
| `design-system/dist/` | generated / untracked | Built `@ds` bundle consumed by `ui/`. |
| `design-system/src/` | tracked | Component + token sources. |
| `docs/architecture/` | tracked | Locked design and DB schemas. |
| `docs/archive/` | tracked | Frozen history — what was believed at the time, current truth about nothing. Exempt from the ownership rules; never edited to 'correct' it. |
| `docs/generated/` | tracked | Machine-generated docs (this file included). Never hand-edited. |
| `docs/implementation/` | tracked | Per-domain implementation invariants — where the code's explanatory comments live instead. |
| `docs/principles/` | tracked | One file per rule, stable id — the imperative form of each convention. |
| `scripts/gates/` | tracked | One module per gate, run by `check.sh`; a failure names the principle file it enforces. |
| `scripts/hooks/` | tracked | Git hooks (`pre-commit`, `pre-push`) plus `install.sh` to wire them up. |
| `ui/dist/` | generated / untracked | Vite build output left in the tree by the `build` gate. Minified bundles — search with `rg` (honours .gitignore), never `grep -r`. |
| `ui/src/` | tracked | Frontend sources. |

## Top-level files (tracked)

- `.gitattributes`
- `.gitignore`
- `CLAUDE.md`
- `Cargo.lock`
- `Cargo.toml`
- `LICENSE`
- `README.md`
- `prd.md`
- `rust-toolchain.toml`
- `skills-lock.json`
